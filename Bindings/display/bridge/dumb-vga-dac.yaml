# SPDX-License-Identifier: None
%YAML 1.2
---
$id: http://devicetree.org/schemas/Bindings/display/bridge/dumb-vga-dac.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#
version: 1

title: Dumb RGB to VGA DAC bridge

maintainers:
  - Maxime Ripard <maxime.ripard@free-electrons.com>
description: |+
  This binding is aimed for dumb RGB to VGA DAC based bridges that do not require
  any configuration.

             

properties:
  compatible: {}
historical: |+
  Dumb RGB to VGA DAC bridge
  ---------------------------

  This binding is aimed for dumb RGB to VGA DAC based bridges that do not require
  any configuration.

  Required properties:

  - compatible: Must be "dumb-vga-dac"

  Required nodes:

  This device has two video ports. Their connections are modelled using the OF
  graph bindings specified in Documentation/devicetree/bindings/graph.txt.

  - Video port 0 for RGB input
  - Video port 1 for VGA output

  Optional properties:
  - vdd-supply: Power supply for DAC

...
