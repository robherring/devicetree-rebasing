# SPDX-License-Identifier: None
%YAML 1.2
---
$id: http://devicetree.org/schemas/Bindings/timer/energymicro,efm32-timer.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#
version: 1

title: EFM32 timer hardware

maintainers:
  - Uwe Kleine-König <u.kleine-koenig@pengutronix.de>
description: |+
  The efm32 Giant Gecko SoCs come with four 16 bit timers. Two counters can be
  connected to form a 32 bit counter. Each timer has three Compare/Capture
  channels and can be used as PWM or Quadrature Decoder. Available clock sources
  are the cpu's HFPERCLK (with a 10-bit prescaler) or an external pin.

             

properties:
  compatible:
    items:
      - const: energymicro,efm32-timer
    minItems: 1
    maxItems: 1
    additionalItems: false
  reg:
    minItems: 1
    maxItems: 1
    additionalItems: false
  interrupts: {}
  clocks: {}
historical: |+
  * EFM32 timer hardware

  The efm32 Giant Gecko SoCs come with four 16 bit timers. Two counters can be
  connected to form a 32 bit counter. Each timer has three Compare/Capture
  channels and can be used as PWM or Quadrature Decoder. Available clock sources
  are the cpu's HFPERCLK (with a 10-bit prescaler) or an external pin.

  Required properties:
  - compatible : Should be "energymicro,efm32-timer"
  - reg : Address and length of the register set
  - clocks : Should contain a reference to the HFPERCLK

  Optional properties:
  - interrupts : Reference to the timer interrupt

...
