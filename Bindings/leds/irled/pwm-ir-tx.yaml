# SPDX-License-Identifier: None
%YAML 1.2
---
$id: http://devicetree.org/schemas/Bindings/leds/irled/pwm-ir-tx.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#
version: 1

title: Device tree bindings for IR LED connected through pwm pin which is used as

maintainers:
  - Sean Young <sean@mess.org>
description: test

properties:
  compatible: {}
examples:
  - |
    irled {
      compatible = "pwm-ir-tx";
      pwms = <&pwm0 0 10000000>;
    };
historical: |+
  Device tree bindings for IR LED connected through pwm pin which is used as
  remote controller transmitter.

  Required properties:
  	- compatible: should be "pwm-ir-tx".
  	- pwms : PWM property to point to the PWM device (phandle)/port (id)
  	  and to specify the period time to be used: <&phandle id period_ns>;

...
