# SPDX-License-Identifier: None
%YAML 1.2
---
$id: http://devicetree.org/schemas/Bindings/rtc/rtc-opal.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#
version: 1

title: IBM OPAL real-time clock

maintainers:
  - Neelesh Gupta <neelegup@linux.vnet.ibm.com>
description: test

properties:
  compatible: {}
examples:
  - |
    rtc {
      compatible = "ibm,opal-rtc";
      wakeup-source;
      phandle = <0x10000029>;
      linux,phandle = <0x10000029>;
    };
historical: |+
  IBM OPAL real-time clock
  ------------------------

  Required properties:
  - compatible: Should be "ibm,opal-rtc"

  Optional properties:
  - wakeup-source: Decides if the wakeup is supported or not
  		 (Legacy property supported: "has-tpo")

...
