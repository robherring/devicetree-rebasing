# SPDX-License-Identifier: None
%YAML 1.2
---
$id: http://devicetree.org/schemas/Bindings/c6x/soc.yaml#
$schema: http://devicetree.org/meta-schemas/core.yaml#
version: 1

title: C6X System-on-Chip

maintainers:
  - Mark Salter <msalter@redhat.com>
description: test

properties:
  compatible: {}
examples:
  - |
    soc {
      compatible = "simple-bus";
      model = "tms320c6455";
      #address-cells = <1>;
      #size-cells = <1>;
      ranges;

      ...
    };
historical: |+
  C6X System-on-Chip
  ------------------

  Required properties:

  - compatible: "simple-bus"
  - #address-cells: must be 1
  - #size-cells: must be 1
  - ranges

  Optional properties:

  - model: specific SoC model

  - nodes for IP blocks within SoC


...
